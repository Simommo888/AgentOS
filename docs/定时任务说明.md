# 定时任务说明

AgentOS v1 使用 APScheduler 的本地单进程调度。

## cron 表达式

使用 5 段 cron：

```text
分 时 日 月 周
```

示例：

- `0 9 * * *`：每天 09:00
- `30 18 * * 1-5`：工作日 18:30
- `*/30 * * * *`：每 30 分钟

## 恢复任务

后端启动时会读取数据库中 `enabled=true` 的 Schedule，并恢复到 APScheduler。

## 手动刷新

创建、编辑、启用或禁用定时任务后，后端会刷新 APScheduler job。
